<!--pages/index/index.wxml-->
<view class="container">
  <view class="deck-list" wx:if="{{decks.length > 0}}">
    <view class="deck-item" wx:for="{{decks}}" wx:key="id">
      <view class="deck-content" data-id="{{item.id}}" bind:tap="navigateToStudy">
        <view class="deck-name">{{item.name}}</view>
        <view class="deck-info">{{item.cards.length}} 张卡片</view>
      </view>
      <view class="deck-actions">
        <icon name="edit" fontSize="42rpx" customClass="action-icon" data-id="{{item.id}}" catch:tap="navigateToEdit" />
        <icon name="trash" fontSize="42rpx" customClass="action-icon" data-id="{{item.id}}" catch:tap="showDeleteModal" />
      </view>
    </view>
  </view>

  <view class="empty-state" wx:else>
    <icon name="inbox-o" fontSize="100rpx" customClass="empty-icon" />
    <text class="empty-text">还没有卡片集</text>
    <text class="empty-tip">点击下方按钮创建一个吧！</text>
  </view>

  <button class="add-btn" bind:tap="navigateToAdd">
    <icon name="add" fontSize="40rpx" customClass="add-btn-icon"/>
    <text>创建新卡片集</text>
  </button>
</view>
