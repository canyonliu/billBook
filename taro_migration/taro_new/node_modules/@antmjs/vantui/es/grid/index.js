import _defineProperty from "@babel/runtime/helpers/defineProperty";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import _slicedToArray from "@babel/runtime/helpers/slicedToArray";
var _excluded = ["gutter", "clickable", "columnNum", "center", "border", "direction", "iconSize", "square", "reverse", "className", "style"];
function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); enumerableOnly && (symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; })), keys.push.apply(keys, symbols); } return keys; }
function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = null != arguments[i] ? arguments[i] : {}; i % 2 ? ownKeys(Object(source), !0).forEach(function (key) { _defineProperty(target, key, source[key]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } return target; }
import { View } from '@tarojs/components';
import { cloneElement, useCallback, useMemo, useRef, useEffect, isValidElement, Children, useState } from 'react';
import * as utils from '../wxs/utils';
import { get } from '../default-props';
import * as computed from './wxs';
import { jsx as _jsx } from "react/jsx-runtime";
export function Grid(props) {
  var _useState = useState(get().Grid),
    _useState2 = _slicedToArray(_useState, 1),
    d = _useState2[0];
  var _d$props = _objectSpread(_objectSpread({}, d), props),
    _d$props$gutter = _d$props.gutter,
    gutter = _d$props$gutter === void 0 ? null : _d$props$gutter,
    clickable = _d$props.clickable,
    _d$props$columnNum = _d$props.columnNum,
    columnNum = _d$props$columnNum === void 0 ? 4 : _d$props$columnNum,
    _d$props$center = _d$props.center,
    center = _d$props$center === void 0 ? true : _d$props$center,
    _d$props$border = _d$props.border,
    border = _d$props$border === void 0 ? true : _d$props$border,
    direction = _d$props.direction,
    _d$props$iconSize = _d$props.iconSize,
    iconSize = _d$props$iconSize === void 0 ? '48' : _d$props$iconSize,
    square = _d$props.square,
    _d$props$reverse = _d$props.reverse,
    reverse = _d$props$reverse === void 0 ? false : _d$props$reverse,
    _d$props$className = _d$props.className,
    className = _d$props$className === void 0 ? '' : _d$props$className,
    _d$props$style = _d$props.style,
    style = _d$props$style === void 0 ? {} : _d$props$style,
    others = _objectWithoutProperties(_d$props, _excluded);
  var childrenInstance = useRef([]);
  var updateChildren = useCallback(function () {
    childrenInstance.current.forEach(function (child) {
      child.updateStyle();
    });
  }, []);
  useEffect(function () {
    updateChildren();
  }, [updateChildren]);
  var setChildrenInstance = useCallback(function (index, instance) {
    childrenInstance.current[index] = instance;
  }, []);
  var ResetChildren = useMemo(function () {
    var res = [];
    if (others.children && Array.isArray(others.children)) {
      Children.map(others.children, function (child, index) {
        if (child && /*#__PURE__*/isValidElement(child)) {
          res.push( /*#__PURE__*/cloneElement(child, {
            setChildrenInstance: setChildrenInstance,
            key: index,
            index: index,
            parentInstance: {
              columnNum: columnNum,
              border: border,
              square: square,
              gutter: gutter,
              clickable: clickable,
              center: center,
              direction: direction,
              reverse: reverse,
              iconSize: iconSize
            }
          }));
        } else {
          res.push(child);
        }
      });
    }
    return res;
  },
  // eslint-disable-next-line react-hooks/exhaustive-deps
  [others.children, columnNum]);
  return /*#__PURE__*/_jsx(View, {
    className: 'van-grid  ' + (border && !gutter ? 'van-hairline--top' : '') + ' ' + className,
    style: utils.style([computed.rootStyle({
      gutter: gutter
    }), style]),
    children: ResetChildren
  });
}
export default Grid;