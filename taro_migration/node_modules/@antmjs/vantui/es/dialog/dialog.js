import _defineProperty from "@babel/runtime/helpers/defineProperty";
function ownKeys(object, enumerableOnly) { var keys = Object.keys(object); if (Object.getOwnPropertySymbols) { var symbols = Object.getOwnPropertySymbols(object); enumerableOnly && (symbols = symbols.filter(function (sym) { return Object.getOwnPropertyDescriptor(object, sym).enumerable; })), keys.push.apply(keys, symbols); } return keys; }
function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = null != arguments[i] ? arguments[i] : {}; i % 2 ? ownKeys(Object(source), !0).forEach(function (key) { _defineProperty(target, key, source[key]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function (key) { Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key)); }); } return target; }
import { on, off, trigger } from './events';
var _defaultOptions = {
  show: false,
  title: '',
  width: null,
  theme: 'default',
  message: '',
  overlay: true,
  className: '',
  asyncClose: false,
  transition: 'scale',
  messageAlign: '',
  overlayStyle: '',
  confirmButtonText: '确认',
  cancelButtonText: '取消',
  showConfirmButton: true,
  showCancelButton: false,
  closeOnClickOverlay: false,
  confirmButtonOpenType: ''
};
export default {
  defaultOptions: _objectSpread({}, _defaultOptions),
  alert: function alert(options) {
    var p = new Promise(function (resolve) {
      var confirmFn = function confirmFn() {
        off('confirm', confirmFn);
        resolve('confirm');
      };
      var cancelFn = function cancelFn() {
        off('cancel', cancelFn);
        resolve('cancel');
      };
      on('confirm', confirmFn);
      on('cancel', cancelFn);
    });
    var innerOptions = (options === null || options === void 0 ? void 0 : options.theme) === 'round-button' ? {
      confirmButtonColor: '#FFFFFF',
      cancelButtonColor: '#FFFFFF'
    } : {};
    trigger('alert', _objectSpread(_objectSpread(_objectSpread(_objectSpread({}, this.defaultOptions), options), innerOptions), {}, {
      show: true
    }));
    return p;
  },
  confirm: function confirm(options) {
    return this.alert(_objectSpread(_objectSpread({}, options), {}, {
      showCancelButton: true
    }));
  },
  close: function close() {
    off('confirm');
    off('cancel');
    trigger('close');
  },
  stopLoading: function stopLoading() {
    trigger('stopLoading');
  },
  setDefaultOptions: function setDefaultOptions(options) {
    ;
    this.defaultOptions = _objectSpread(_objectSpread({}, this.defaultOptions), options);
  },
  resetDefaultOptions: function resetDefaultOptions() {
    ;
    this.defaultOptions = _objectSpread({}, _defaultOptions);
  }
};