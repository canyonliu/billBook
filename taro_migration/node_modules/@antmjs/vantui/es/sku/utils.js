export function everyItemEqual(a, b) {
  var equalLength = 0;
  for (var i = 0; i < a.length; i++) {
    var itema = a[i];
    if (itema && b.includes(itema)) equalLength++;
  }
  return equalLength === b.length;
}